services:
  - type: web
    name: cipher-audit-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: AUDIT_API_TOKEN
        sync: false
      - key: ALLOWED_ORIGIN
        sync: false
